import 'package:flutter/material.dart';

class CirclurProgressIndicator extends StatelessWidget {
  const CirclurProgressIndicator({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
